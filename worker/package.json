{
  "name": "demo-agent-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "d1:migrate": "wrangler d1 execute demo-agent-db --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "openai": "^4.70.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250921.0",
    "wrangler": "^4.0.0"
  }
}
